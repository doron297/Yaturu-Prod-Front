import { OnInit, OnChanges, SimpleChanges } from '@angular/core';
import { CloudinaryOptions } from './cloudinary-options.class';
import { CloudinaryTransforms } from './cloudinary-transforms.class';
import { CloudinaryImageService } from './cloudinary-image.service';
export declare class CloudinaryImageComponent implements OnInit, OnChanges {
    private _imageService;
    imageUrl: string;
    transforms: CloudinaryTransforms;
    publicId: string;
    options: CloudinaryOptions;
    title: string;
    alt: string;
    constructor(_imageService: CloudinaryImageService);
    ngOnInit(): void;
    ngOnChanges(changes: SimpleChanges): void;
    format: string;
    height: string;
    width: string;
    crop: string;
    gravity: string;
    x: string;
    y: string;
}
